<UserControl x:Class="Go212.POS.Desktop.Controls.NumericKeypad"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="280" d:DesignWidth="240">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Row 0: 7, 8, 9 -->
        <Button Grid.Row="0" Grid.Column="0" Content="7" Tag="7" Click="OnDigitClicked" Style="{StaticResource KeypadButton}" Margin="2"/>
        <Button Grid.Row="0" Grid.Column="1" Content="8" Tag="8" Click="OnDigitClicked" Style="{StaticResource KeypadButton}" Margin="2"/>
        <Button Grid.Row="0" Grid.Column="2" Content="9" Tag="9" Click="OnDigitClicked" Style="{StaticResource KeypadButton}" Margin="2"/>

        <!-- Row 1: 4, 5, 6 -->
        <Button Grid.Row="1" Grid.Column="0" Content="4" Tag="4" Click="OnDigitClicked" Style="{StaticResource KeypadButton}" Margin="2"/>
        <Button Grid.Row="1" Grid.Column="1" Content="5" Tag="5" Click="OnDigitClicked" Style="{StaticResource KeypadButton}" Margin="2"/>
        <Button Grid.Row="1" Grid.Column="2" Content="6" Tag="6" Click="OnDigitClicked" Style="{StaticResource KeypadButton}" Margin="2"/>

        <!-- Row 2: 1, 2, 3 -->
        <Button Grid.Row="2" Grid.Column="0" Content="1" Tag="1" Click="OnDigitClicked" Style="{StaticResource KeypadButton}" Margin="2"/>
        <Button Grid.Row="2" Grid.Column="1" Content="2" Tag="2" Click="OnDigitClicked" Style="{StaticResource KeypadButton}" Margin="2"/>
        <Button Grid.Row="2" Grid.Column="2" Content="3" Tag="3" Click="OnDigitClicked" Style="{StaticResource KeypadButton}" Margin="2"/>

        <!-- Row 3: C, 0, . -->
        <Button Grid.Row="3" Grid.Column="0" Content="C" Click="OnClearClicked" Style="{StaticResource KeypadDangerButton}" Margin="2"/>
        <Button Grid.Row="3" Grid.Column="1" Content="0" Tag="0" Click="OnDigitClicked" Style="{StaticResource KeypadButton}" Margin="2"/>
        <Button Grid.Row="3" Grid.Column="2" Content="." Tag="." Click="OnDigitClicked" Style="{StaticResource KeypadButton}" Margin="2"/>
    </Grid>
</UserControl>
